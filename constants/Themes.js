export const getBackgroundTheme = (isLight) => {
  return isLight ? "#f4f4f4" : "#1b1b1b";
};

export const getBorderColorTheme = (isLight) => {
  return isLight ? "black" : "#1b1b1b";
};

export const textColorTheme = (isLight) => {
  return isLight ? "black" : "white";
};
